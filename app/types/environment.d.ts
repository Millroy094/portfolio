namespace NodeJS {
    // eslint-disable-next-line @typescript-eslint/consistent-type-definitions -- Need to use interface here to extend the existing NodeJS.ProcessEnv
    interface ProcessEnv {
        PUBLIC_URL: string;
    }
}
